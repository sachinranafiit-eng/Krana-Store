const fs=require('fs'),path=require('path'),Excel=require('exceljs'),schemas=require('../shared/import-schemas.json');
(async()=>{const dir=path.resolve(__dirname,'../templates');fs.mkdirSync(dir,{recursive:true});for(const[k,s]of Object.entries(schemas)){const csv=s.columns.join(',')+'\n'+s.columns.map(k=>'"'+String(s.example[k]??'').replaceAll('"','""')+'"').join(',')+'\n';fs.writeFileSync(path.join(dir,k+'-template.csv'),csv);const w=new Excel.Workbook(),sheet=w.addWorksheet(s.label);sheet.addRow(s.columns);sheet.addRow(s.columns.map(k=>s.example[k]??''));sheet.getRow(1).font={bold:true,color:{argb:'FFFFFFFF'}};sheet.getRow(1).fill={type:'pattern',pattern:'solid',fgColor:{argb:'FF087C45'}};sheet.columns.forEach(c=>c.width=24);await w.xlsx.writeFile(path.join(dir,k+'-template.xlsx'));}console.log('Generated 8 bulk templates');})();
