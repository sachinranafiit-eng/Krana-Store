const fs=require('fs');const path=require('path');const {PGlite}=require('@electric-sql/pglite');const {pg_trgm}=require('@electric-sql/pglite/contrib/pg_trgm');
(async()=>{const [file,destination]=process.argv.slice(2);if(!file||!destination)throw Error('Usage: node scripts/restore.js backup.tar.gz NEW_DIRECTORY');const dir=path.resolve(destination);if(fs.existsSync(dir))throw Error('Destination already exists. Choose a new directory.');const db=new PGlite({dataDir:dir,loadDataDir:new Blob([fs.readFileSync(file)]),extensions:{pg_trgm}});console.log((await db.query('SELECT count(*) AS products FROM products')).rows[0]);await db.close();console.log('Restored into '+dir);})().catch(e=>{console.error(e.message);process.exit(1);});
